replicaCount: 1
namespace: ""

image:
  repository: docker
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: "test-roadrunner"

podAnnotations: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
# runAsUser: 1000

service:
  type: LoadBalancer
  service_443:
    name: "https"
    port: "443"
  service_80:
    name: "http"
    port: "80"

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
   limits:
     cpu: 1024m
     memory: 2048Mi


autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}


global:
  env: test
  container:
     image: datasahi/roadrunnr-proxy
     tag: '20230219'

  jvm:
    xmx: "1024m"
    newgen_min: "256m"
    newgen_max: "512m"
  jdbc_read:
    host: 'jdbc:postgresql://testing-do-user-5069280-0.b.db.ondigitalocean.com:25061/rrproxy?sslmode=require'
    username: 'roadrunner-proxy'
    password: 'AVNS_jKXVi9V3yuQvw0W7-qA'
    driver: "org.postgresql.Driver"
    max_connections: '1'
  jdbc_write:
    host: 'jdbc:postgresql://testing-do-user-5069280-0.b.db.ondigitalocean.com:25061/rrproxy?sslmode=require'
    username: 'roadrunner-proxy'
    password: 'AVNS_jKXVi9V3yuQvw0W7-qA'
    driver: "org.postgresql.Driver"
    max_connections: '1'
  http_port:
    port: '8080'
  audit:
    kafka_enabled: 'false'
  url:
    roadrunner_url: '139.59.49.203'
    port: '5050'
  proxy:
    id: ''
  protocol:
    enabled: 'true'

      





